{
  "name": "@piyoppi/counter",
  "version": "0.1.2",
  "description": "Simple counter tools for web pages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "(cd packages/counter && npm test) && (cd packages/url-whitelist-sitemap && npm test) && (cd packages/actions-upload-url-whitelist-to-s3 && npm test)"
  },
  "author": "piyoppi"
}
